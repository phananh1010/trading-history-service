A stand-alone market screener that 
(a) builds a universe (Nasdaq API), 
(b) uses HistoryService + TimeSeriesCache to fetch 180-day daily OHLCV, and 
(c) computes several metrics, then dumps ad-hoc sorted lists.

